apiVersion: v1
kind: Service
metadata:
  name: mediturno-service
  labels:
    app: mediturno
spec:
  type: ClusterIP
  selector:
    app: mediturno
  ports:
    - name: http
      port: 80
      targetPort: 5000
