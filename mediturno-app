pipeline {
    agent any

    tools {
        nodejs 'NodeJS_20'
    }

    stages {
        stage('Verificar Node') {
            steps {
                sh 'node --version'
                sh 'npm --version'
            }
        }
    }
}
